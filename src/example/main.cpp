#include <iostream>
#include "../lib/sklaga.h"

const int WINDOW_WIDTH = 1280;
const int WINDOW_HEIGHT = 720;

int main() {
    std::cout << "Hello" << std::endl;
    init_window(WINDOW_WIDTH, WINDOW_HEIGHT, "sklaga example");
}